# AI REPORT 

I used ai for the flexbox layout for the bottom part responsiveness, because i could not do it with html tables, so i prompted this
```
change this section to be responsive (insert table snippet here), change columns to rows on mobile
```
